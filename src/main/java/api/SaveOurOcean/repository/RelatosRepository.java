package api.SaveOurOcean.repository;

import api.SaveOurOcean.model.Relato;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RelatosRepository extends JpaRepository<Relato, Long> {
}
